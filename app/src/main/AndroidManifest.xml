<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifpi.controlmedv2">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NovoPacienteActivity" />
        <activity android:name=".ListPacienteActivity" />
        <activity android:name=".MedicamentosActivity" />
        <activity android:name=".AgendaActivity" />
        <activity android:name=".HistoricoActivity" />
        <activity android:name=".AnaliseActivity" />
        <!--
             ATTENTION: This was auto-generated to add Google Play services to your project for
             App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".NovoMedicamento2Activity" />
        <activity
            android:name=".NovaConsultaActivity"
            android:label="@string/title_activity_nova_consulta" />
        <activity
            android:name=".NovoExameActivity"
            android:label="@string/title_activity_novo_exame" />
        <activity
            android:name=".NovoMedicamentoActivity"
            android:label="@string/title_activity_novo_medicamento"></activity>
    </application>

</manifest>
